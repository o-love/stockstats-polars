[project]
name = "stockstats-polars-dev"
version = "0.1.0"
description = "Development tooling (maturin, pytest) for the stockstats-polars workspace."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "maturin>=1.10,<2.0",
    "pytest>=8.3",
]
